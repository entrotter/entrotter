# Validate real Anvil execution and cleanup

Run the supplied integration test with pinned Foundry. Add tests for rejected txs, failed startup and timeout cleanup. Attach real receipts and logs. Do not use a mock to satisfy this gate.

## Acceptance

- Reproduction commands and actual output are attached.
- Tests and README/limitations are updated.
- PR is focused and links dependent repository changes.
