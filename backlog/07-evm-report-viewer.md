# Render EVM reports safely in the static explorer

Render source pins, receipts and native/token deltas with appropriate units. No innerHTML from files, no file uploads, no unsafe links. Test malformed artifacts and mobile layout.

## Acceptance

- Reproduction commands and actual output are attached.
- Tests and README/limitations are updated.
- PR is focused and links dependent repository changes.
